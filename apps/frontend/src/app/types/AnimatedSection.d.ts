interface AnimatedSectionData {
  name: string;
  component: React.ReactNode;
  scrollRange: [number, number];
  yRange: [number, number];
  opacityRange: [number, number];
  scaleRange: [number, number];
}
